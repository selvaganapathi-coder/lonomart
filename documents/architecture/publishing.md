# Publishing Architecture

Publishing is not implemented in TASK-001.

Future publishing work should define draft and published states, immutable publication artifacts, rollback behavior, and cache invalidation before writing production code.
